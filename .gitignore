# Netlify link state — machine-specific
.netlify/

# Never commit: screenshots go in only after the redaction pass
assets/screenshots/

# Python
__pycache__/
*.pyc
.venv/

# OS
Thumbs.db
.DS_Store

# Private blocklist for leakcheck.py — names of individuals, never committed
leakcheck_private.py
field-guide.locked.html

# The field guide is personal: never in this public repo. It deploys separately,
# encrypted, to its own Netlify site (see ../SITE-NOTES.md).
field-guide.html
lock-fieldguide.js
